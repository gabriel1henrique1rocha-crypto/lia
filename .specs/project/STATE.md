# State — LIA


**Last Updated:** 2026-08-26

**Current Work:** **M0, M1, M2 e M3 concluídos, tudo em produção.** M3 (`reviews-crud`): **T1–T8 e T10–T12 entregues**; **T9 removida** por D-11 (a nota saiu do produto — coluna `rating` dropada pela 0010, D-11/D-12 em Recent Decisions/Todos). **T13** (ensaio final: gate completo + roteiro NVDA + checklist de merge) segue com o próprio checklist do `tasks.md` **não marcado** — boa parte do que ela pedia aconteceu de fato ao longo de 2026-08-26 (gate full verde repetidas vezes, `build`, smoke test real em produção), mas o roteiro manual com leitor de tela não tem registro em lugar nenhum; **tratar como pendência, não como concluída** (divergência encontrada nesta reconciliação — ver nota em Todos §M3).

Migrations **0009, 0010 e 0011 aplicadas em produção** (`supabase migration list --linked` confirma local=remote nas onze migrations, 2026-08-26). **Primeira resenha real** criada pelo formulário e publicada — **`o-projeto-rosie`** (2026-08-26). Verificado nesta reconciliação: produção tem hoje **5 resenhas publicadas + 2 rascunhos** — o segundo rascunho, `marquez-gabriel-garcia-cem-anos-de-solidao-rio-de-janeiro-record-2012`, não estava registrado em lugar nenhum antes desta passada; fica anotado como fato observado, sem inventar quem o criou ou quando.

E-mail transacional em produção: **SMTP via Resend**, domínio `observatorioolda.com.br` verificado (DKIM/SPF/MX/DMARC no hPanel da Hostinger), template do Magic Link corrigido para o fluxo `token_hash`, `Site URL` apontando para o domínio de produção. **Dois admins reais** provisionados pelo caminho privilegiado do runbook §2 (SQL, não `service_role`) — confirmado por query direta: 2 linhas em `public.editor`, `role='admin'`, `active=true`, ambas com `auth.users` correspondente.

**TD-02 FECHADA por completo**: as 7 suítes `*.integration.test.ts` rodam no CI (`rls (supabase local)`) e a branch protection de `main` está **ativa**, verificada via `gh api .../branches/main/protection` — 4 checks obrigatórios (`lint + format + types`, `vitest`, `rls (supabase local)`, `axe + lighthouse`), `strict: true`, 1 aprovação exigida. **ACHADO ao verificar:** até 2026-08-26 **não existia branch protection nenhuma** em `main` — os merges anteriores (PRs #2 a #21) passavam só por disciplina humana, nunca por bloqueio técnico; premissa contrária havia sido dada como certa em prompts anteriores desta sessão. **TD-11 FECHADA**: 12 execuções NOVAS e distintas (nunca `gh run rerun`) do job de RLS, 12/12 sem falha de bind de porta, depois de reservar `54320-54329` fora do range efêmero do kernel.

**GRANTs verificados diretamente em produção** (leitura pura via `supabase db query --linked`): idênticos ao banco local em todas as 18 combinações tabela×papel — `anon` só com `SELECT` em `book`/`genre`/`review`, nada em `editor`/`comment`/`recommendation`; `service_role` sem DML em nenhuma tabela. **A-2 e a dormência do `service_role` (C-2/D-09) CONFIRMADOS no ar.** O problema de GRANT implícito que a TD-02 descobriu (`auto_expose_new_tables`) era exclusivo do banco EFÊMERO que o CI criava do zero — produção nunca teve o defeito.

**Próximo milestone: M4**, bloqueado pelo vocabulário inicial `[PREENCHER]` de **D-12** (taxonomia de deficiência representada) — sem ele, o passo 3 de D-11 (retirar o filtro por nota da home) não tem substituto e a home ficaria sem eixo editorial nenhum.

---

## Recent Decisions (Last 60 days)

> ADRs completos em [DECISIONS.md](DECISIONS.md). Resumo das resolvidas:

### D-05: Hospedagem na Vercel (2026-06-07)

**Decision:** hospedar na Vercel.
**Reason:** suporte first-class a Next.js (App Router/SSR/SSG), CDN global, deploy contínuo.
**Trade-off:** acoplamento ao ecossistema Vercel.
**Impact:** pipeline de deploy do M0 mira a Vercel.

### D-06: TypeScript sobre base JavaScript (2026-06-07)

**Decision:** adotar TypeScript.
**Reason:** tipar modelo de dados e contratos reduz bugs; respeita a stack "JavaScript".
**Trade-off:** passo de build/tipagem adicional.
**Impact:** projeto inicializado em TypeScript no M0; tipos derivados do schema.

### D-07: Tailwind v4 (@theme) + escala numérica de spacing (2026-06-07)

**Decision:** Tailwind **v4**, tokens em `@theme` como fonte única; spacing via `--spacing: initial` + chaves `1–9`.
**Reason:** CSS-first satisfaz INFRA-07 nativamente; reset + chaves explícitas resolvem a colisão da INFRA-08.
**Trade-off:** v4 mais novo; `p-8`=64px diverge da convenção numérica do Tailwind (documentado).
**Impact:** sem `tailwind.config.js`; componentes consomem só tokens. ADR completa em [DECISIONS.md](DECISIONS.md) D-07.

### D-08: Domínio canônico de produção — www.observatorioolda.com.br (2026-07-30)

**Decision:** domínio canônico de produção **`www.observatorioolda.com.br`**, registrado na Hostinger (substitui `www.literaturainclusiva.com.br`).
**Reason:** DNS validado e SSL válido; o domínio anterior nunca saiu de "Invalid Configuration" na Vercel.
**Trade-off:** nenhum — resolução de pendência de DNS, não escolha entre alternativas técnicas.
**Impact:** `NEXT_PUBLIC_SITE_URL` de Production atualizada para o novo domínio — impacta `og:url`/canonical (via `metadataBase`) e o futuro sitemap (`seo-core`). ADR completa em [DECISIONS.md](DECISIONS.md) D-08. **Fechada** — sem pendência de DNS.

---

## Technical Debt

| ID | Descrição | Severidade | Milestone sugerido |
| --- | --- | --- | --- |
| TD-01 | T-06/T-07 marcou os 4 componentes base como `'use client'` por causa do `useId` no `Field`. `Button`, `Link` e `Card` são candidatos a Server Component (sem hooks); separar reduz JS enviado ao cliente e melhora Core Web Vitals (TBT/INP). | Leve | M4 |
| TD-02 | **RESOLVIDA em 2026-08-26** (`ci/rls-supabase`) — o gatilho foi disparado (2º admin real em produção) e as **sete** suítes `*.integration.test.ts` passaram a rodar no CI, no job **`rls (supabase local)`**. *(O registro anterior listava seis; a sétima é `src/lib/review/__tests__/rls.integration.test.ts` — RLS de `review`, RVW-13/14/15.)* **Como:** `supabase/setup-cli` (versão PINADA 2.116.0) + `supabase start` com dez serviços excluídos por `-x`; `services:` com Postgres puro foi descartado porque as suítes exercitam sessões autenticadas REAIS via GoTrue e a Data API do PostgREST/Kong — sem esses containers o que se testaria seria `set role` em psql, que não é o caminho de produção. Credenciais lidas de `supabase status -o env` do stack efêmero (nada hardcoded, nada em secret); o nome do container do oráculo é DESCOBERTO por `docker ps` e injetado em `SUPABASE_LOCAL_DB_CONTAINER`, a variável de override que as suítes já liam — **nenhuma suíte foi tocada** e o default local (`supabase_db_lia`) segue valendo. Guarda de seed confere 4 published + 1 draft e falha em uma linha se faltar. **Resultado: 6 execuções consecutivas verdes**, 7 arquivos / 51 passed / 1 skipped, idêntico à linha de base local; job de 2m04s a 2m10s. **RESTA (ação humana, não automatizável):** marcar `rls (supabase local)` como required check em Settings → Branches — ver `docs/CI.md`. A TD-11 fechou (12/12 execuções verdes, ver TD-11) e a branch protection foi ligada em 2026-08-26/27: verificado via `gh api repos/gabriel1henrique1rocha-crypto/lia/branches/main/protection` que os 4 checks (`lint + format + types`, `vitest`, `rls (supabase local)`, `axe + lighthouse`) são **required**, `strict: true` (branch precisa estar atualizada) e 1 aprovação é exigida. **FECHADA POR COMPLETO.** **ACHADO separado, sem relação com o mecanismo do CI:** antes desta data **não existia branch protection nenhuma em `main`** — os 21 PRs mergeados até aqui passaram só por disciplina humana, nunca por bloqueio técnico; uma premissa anterior desta sessão dava a proteção como já existente, o que era falso. **Achado colhido no caminho:** `listing.integration` dependia de `NEXT_PUBLIC_SUPABASE_*` sem declarar — localmente satisfeito pelo `.env.local`, onde essas variáveis apontam para o projeto **REMOTO**; no CI elas apontam para o efêmero, o que é estritamente mais seguro. Média | gatilho: pré-2º-editor |
| TD-03 | **Pós-2026-05-30, o Supabase não auto-concede GRANTs a tabelas novas** do schema public. Policies RLS **não bastam** sem GRANT de tabela — o Data API retorna 42501. **REDUZIDA em `security-foundation`** (2026-07-08): 0007 concede `select/insert/update` de `editor` a `authenticated` (+ policies RBAC); 0008 concede `insert/update/delete` de `review` a `authenticated` (+ policies own-or-admin). **Remanescentes (AINDA ABERTOS):** (1) `comment` — GRANTs/policies de escrita+moderação → `public-comments`/`admin-comment-moderation` (M3); (2) `recommendation` → `recommendations` (M3); (3) **GRANTs de `service_role`/Data API** — deliberadamente AUSENTES (dormência C-2/D-09; provado por T16: service_role toma 42501 em editor/review); só com a 1ª exceção real de bypass, com GRANT MÍNIMO + ADR (ver [runbook §4](../../docs/runbook-admin-bootstrap.md)); (4) DELETE de `review` a editor (hoje admin-only) se o produto exigir → `admin-reviews`; (5) policies de Storage por papel → feature de Storage (M2). O caso (3) `it.skip` do rls.integration antigo segue skip por isto (service_role sem grant é o estado projetado). | **Média** (reduzida de Alta) | M3 / conforme feature |
| TD-04 | **FECHADA em `security-foundation`** (2026-07-08, aguardando merge). O fallback `SUPABASE_SERVICE_ROLE_KEY ?? PUBLISHABLE_KEY` do antigo `server.ts` deixou de existir (arquivo removido); o caminho público usa `createPublicClient` (anon, só publishable) sem rota de import até a service_role (grafo de imports + `server-only` + lint allowlist vazia + tripwire estático). Regressão SEC-14 prova em runtime que, **mesmo com `SUPABASE_SERVICE_ROLE_KEY` presente no ambiente**, o caminho público lê só `published` (prova de vermelhidão executada). Gate SEC-17 mantém a chave fora de Production. **Fecha de fato no merge do PR + confirmação do checklist Vercel.** | ~~Média~~ **fechada** | M2 (fechada) |
| TD-05 | **FECHADA (2026-08-09).** `.gitattributes` não tinha `eol=lf` explícito para `.mjs`/`.cjs` (só `.js`/`.jsx` estavam cobertos). No checkout Windows, `text=auto` entregava EOL nativo (CRLF) para esses dois arquivos e o `format:check` (Prettier) falhava localmente em `eslint.config.mjs` e `scripts/e2e-magic-link.mjs` — o CI (Linux) sempre passou, então nunca bloqueou merge, mas quebrava o gate local a cada checkout. Os blobs no git já eram LF (confirmado: `git add --renormalize .` não tocou nenhum blob); corrigido adicionando `*.mjs text eol=lf` e `*.cjs text eol=lf` e forçando o working tree a refletir o atributo. `format:check` passa localmente no Windows agora. | Leve | — (fechada) |
| TD-06 | **(2026-08-24)** Home-hub vs `/catalogo` como rota própria. A nova arquitetura de informação pública (PR #8) tem "Catálogo" no header apontando para `/`, porque hoje a home **É** a listagem de resenhas (M1) — não existe rota própria. Na intenção da IA nova a home deveria virar um hub, com o catálogo migrando para sua própria rota. Decidir no M4. | Média | M4 |
| TD-07 | **(2026-08-24)** O header público (`SiteNav`, PR #8) é renderizado no layout raiz, portanto aparece também em `/admin/*`. Falta separar o chrome administrativo do chrome público (route groups distintos ou layout condicional). **ESCOPO AMPLIADO na T10 (2026-08-26), medido em `/admin/login`: não é só o header — o `SiteFooter` também vem** (`contentinfo` com "LIA — Leituras e impressões anotadas · © 2026"), porque os dois estão no `RootLayout`. Impacto no teclado MEDIDO e **menor do que parece**: são **8 pontos focáveis antes do `<main>`** (skip link + marca + os 6 destinos) e **0 depois** (o rodapé não tem link nenhum) — e o **skip link continua sendo a 1ª parada e pula o header inteiro de uma vez**, então o custo real para quem tabula é **1 tecla**, não 8. Fixado por teste (`admin-reviews.spec.ts`). O problema remanescente é de CONTEXTO, não de navegação: o painel oferece 6 destinos públicos e um rodapé de site público a quem está editando, e um `contentinfo` do site público numa tela de administração é ruído semântico. **Não corrigido na T10** (fora do escopo). | Leve | a decidir |
| TD-08 | **(2026-08-24)** `app/sitemap.ts` inexistente no projeto. O mapa de requisitos v1.0 lista sitemap automático como obrigatório de MVP — a lacuna é de requisito não atendido, não de inaplicabilidade. Ao criar o sitemap, excluir as 5 páginas placeholder (`/quem-somos`, `/autores`, `/filmografia`, `/liacast`, `/sugestoes`) enquanto seguirem vazias. | Média | gatilho: antes do lançamento (MVP) |
| TD-09 | **(2026-08-24)** `anon` tem **`TRUNCATE`** nas 6 tabelas do `public` (`book`, `comment`, `editor`, `genre`, `recommendation`, `review`) — herdado do `ALTER DEFAULT PRIVILEGES` do Supabase (`anon=Dxtm`), **não concedido por nenhuma migration**. Importa porque **TRUNCATE ignora RLS**: a policy não protege se algum dia for alcançável. **Hoje NÃO é alcançável pelo Data API** — o PostgREST só emite SELECT/INSERT/UPDATE/DELETE e chamadas de função, nunca TRUNCATE. Revogar (`revoke truncate on all tables in schema public from anon`) em passada de endurecimento, com verificação de que nada do stack depende disso. Descoberto na corrida de evidências de T1 (E-6). | Baixa (não alcançável hoje) | passada de endurecimento |
| TD-10 | **(2026-08-24)** **Flakiness da suíte Vitest sob contenção de Docker.** Com o stack local do Supabase rodando, observadas duas falhas distintas e não reproduzíveis: (1) coleta parcial — `189/277` testes coletados, 1 arquivo falho + **7 erros de importação**, com `environment 397s`; (2) falha unitária isolada — 277 coletados, `240 passed / 1 failed`, `environment 424s`. Em ambos os casos a reexecução imediata deu **241 passed / 36 skipped (277)**, e as execuções verdes têm `environment` de 176–290s. Correlação clara com contenção de I/O/CPU do Docker Desktop (inclusive um episódio em que o daemon caiu sozinho no meio da sessão). **Não houve alteração de código entre a vermelha e a verde.** Risco real: mascarar uma regressão de verdade como "flaky". Investigar `pool`/`maxConcurrency` do Vitest e isolar a suíte do stack local, ou subir o stack só nas suítes de integração. **EVIDÊNCIA DO CI (2026-08-26, `ci/rls-supabase`): NÃO reproduziu.** Seis execuções consecutivas do job `rls (supabase local)` num runner de 2 vCPU deram 7 arquivos / 51 passed / 1 skipped, com `environment` entre **4,85 s e 7,20 s** — contra os 400 s observados localmente. **CAUSA PROVÁVEL LOCALIZADA, correção NÃO aplicada:** o `vitest.config.ts` impõe `environment: 'jsdom'` a **todos** os arquivos, e as suítes de integração são puramente Node (não tocam DOM). O custo é por ARQUIVO: no CI só 7 arquivos rodam (≈7 s de environment); no `npm test` local rodam 50 (≈250-400 s), e é aí que a contenção de Docker morde. **Correção proposta:** um `environment: 'node'` por-diretório ou via `// @vitest-environment node` nas suítes de integração — tarefa própria, porque mexe na config de teste de todo o projeto e merece medição antes e depois. Enquanto isso, o job do CI já não sofre: dos 11 containers do stack, 7 não sobem (`-x`), e `timeout-minutes: 20` transforma travamento em falha rápida em vez de 6 h queimadas. | Média | a agendar |
| TD-11 | **(2026-08-26, achada ao subir o stack no CI — TD-02)** **`supabase start` falha por disputa de porta em ~1 de cada 7 execuções.** Erro exato: `failed to bind host port for 0.0.0.0:54322:172.18.0.2:5432/tcp: address already in use`. **Não é a TD-10** — não é contenção de CPU/IO nem coleta parcial: os containers nem sobem, as suítes nem começam. **Mecanismo:** as portas do `config.toml` (54320-54324) caem DENTRO do range efêmero do kernel (`net.ipv4.ip_local_port_range`, default 32768-60999), então qualquer conexão de SAÍDA do runner — `npm ci`, `docker pull` — pode tomar 54322 como porta de ORIGEM por alguns segundos e derrubar o bind do Docker. Bate com a taxa observada: 6 execuções verdes seguidas, depois uma falha num commit que só mexeu em documentação. **CORREÇÃO PROPOSTA, NÃO APLICADA** (é tarefa própria, com medição): reservar a faixa antes do `supabase start` — `sudo sysctl -w net.ipv4.ip_local_reserved_ports=54320-54329` —, o que instrui o kernel a nunca entregá-las como porta efêmera. Alternativas piores consideradas: retry cego do `start` (esconde a causa e mascararia falha real de subida) e mudar as portas do `config.toml` (quebraria o ambiente local de todo mundo por um problema que é do runner). Um passo de PRÉ-VOO já foi acrescentado ao job: imprime o range efêmero, as portas reservadas e quem está escutando em 543xx, para a próxima ocorrência chegar com evidência em vez de "o CI falhou sozinho". **FECHADA (2026-08-26).** Corrigido com `sudo sysctl -w net.ipv4.ip_local_reserved_ports=54320-54329` antes do `supabase start`. Medido em **12 execuções NOVAS e distintas** — `git commit --allow-empty` + `git push` um a um, NUNCA `gh run rerun` (rerun reaproveita o runner e não testa a condição de disputa por porta efêmera; uma tentativa anterior usou rerun por engano, 2 execuções descartadas, não contam para o fechamento). **12/12 sucesso**, conferido no nível do workflow E do job `rls (supabase local)` especificamente. `environment` entre **6,69 s e 7,44 s** em todas — dentro da faixa já observada, sem regressão. O pré-voo confirmou `portas reservadas: 54320-54329` nas 12, provando que a reserva pega em runner novo, toda vez. IDs dos 12 runs: 33024478174, 33024483936, 33024490394, 33024495312, 33024500413, 33024505206, 33024510562, 33024515383, 33024521690, 33024528791, 33024534405, 33024540936. **BLOQUEIA tornar `rls (supabase local)` um required check** — ver a nota em TD-02. | Média | antes de virar required check |

---

## Active Blockers

**Nenhum bloqueador.** ✅ *(Deploy da `review-page` RESOLVIDO em 2026-07-05 — `0005` aplicada ao banco de produção LIA + seed rodado; `/resenha/[slug]` responde em produção com as 4 publicadas, draft invisível.)*

### Backlog / polish (não-bloqueante)

- [x] **INFRA — SMTP de produção via Resend + template do Magic Link corrigido (2026-08-26).** Domínio `observatorioolda.com.br` verificado no provedor (DKIM, SPF, MX, DMARC configurados no hPanel da Hostinger); SMTP custom apontado no Supabase Auth (porta 465, remetente do domínio verificado — pré-requisito A-6 de `security-foundation`, antes só documentado). O template do e-mail de Magic Link foi corrigido para o fluxo `token_hash` (o fluxo que `/auth/confirm` de fato consome — ver D-10); `Site URL` do projeto Supabase passou a apontar para o domínio de produção, não mais `localhost`.
- [x] **INFRA — bootstrap dos dois admins reais concluído (2026-08-26).** Pelo caminho privilegiado do runbook ([§2](../../docs/runbook-admin-bootstrap.md)) — SQL direto, **não** `service_role` (a dormência C-2/D-09 continua intacta: nenhum GRANT novo foi concedido para provisionar os dois). Confirmado por query: 2 linhas em `public.editor`, ambas `role='admin'` e `active=true`, ambas com `auth.users` correspondente.
- [x] **ACHADO — até 2026-08-26 NÃO existia branch protection em `main`.** Os 21 PRs mergeados até aqui passaram só por disciplina humana — CI verde era sinal, não bloqueio técnico; nada impedia um push direto ou um merge com CI vermelho. Uma premissa contrária ("a proteção já existe") havia sido assumida em prompts anteriores desta sessão de forma implícita, e era falsa. **Corrigido nesta data**: branch protection ligada com os 4 checks obrigatórios (`lint + format + types`, `vitest`, `rls (supabase local)`, `axe + lighthouse`), `strict: true`, 1 aprovação exigida — confirmado via `gh api repos/gabriel1henrique1rocha-crypto/lia/branches/main/protection`. Ver TD-02.
- [ ] **ACHADO NESTA RECONCILIAÇÃO — segundo rascunho em produção, origem não registrada.** `marquez-gabriel-garcia-cem-anos-de-solidao-rio-de-janeiro-record-2012` existe em `public.review` com `status='draft'`, além das 5 publicadas e do rascunho histórico `memorias-postumas-rascunho`. Não há registro em STATE.md, tasks.md nem git log de quem o criou ou quando — presumivelmente um dos dois admins testando o formulário. Fato observado, não investigado: fica como está, sem inventar autor ou data.
- [ ] **DIVERGÊNCIA ENCONTRADA — T13 (`reviews-crud`) não está formalmente concluída.** O `tasks.md` marca T10/T11/T12 com `— CONCLUÍDA (2026-08-26)` no próprio cabeçalho da task; **T13 não tem essa marca**, e todo o bloco "Done when" segue com checkbox `[ ]` (gate full verde, 4 suítes integration local, roteiro NVDA, `build`, checklist de merge). Nesta reconciliação, boa parte do que T13 pede tem evidência espalhada pela sessão (gate completo rodou verde várias vezes, `build` ok, uma resenha real foi criada e publicada em produção — o próprio smoke test que T13 pede), mas **o roteiro manual com NVDA não aparece registrado em lugar nenhum**, e o checklist de merge do T13 não foi preenchido no `tasks.md`. Registrado aqui como divergência, não corrigido — fechar T13 (rodar o roteiro NVDA que falta e marcar o checklist) é decisão de quem conduz o M3, não desta reconciliação.
- [x] **RESOLVIDA (2026-08-26) EM PRODUÇÃO — `anon` pode ter DML implícito nas seis tabelas do `public`** (achado de 2026-08-26, ao subir o stack no CI). O banco EFÊMERO que o CI cria do zero nascia com `anon`, `authenticated` **e** `service_role` recebendo `SELECT,INSERT,UPDATE,DELETE` em **`book`, `comment`, `editor`, `genre`, `recommendation`, `review`** — sem nenhuma migration conceder isso. Causa: `auto_expose_new_tables` estava IMPLÍCITO no `config.toml`, e o default implícito ainda auto-expõe tabelas novas do `public` criadas por `postgres`. **Corrigido no repositório** (`auto_expose_new_tables = false`, 2026-08-26): o efêmero passou a nascer idêntico ao banco local, com só os GRANTs explícitos das 0004/0005/0007/0008/0009. **O QUE ISSO NÃO RESOLVE:** o banco de PRODUÇÃO foi provisionado antes desta linha existir, e ninguém verificou os GRANTs de lá. Se produção também tiver os implícitos, então (a) o default **A-2** ("`anon` sem GRANT em `editor`/`comment`/`recommendation`") é FALSO no ar — a negação vem só da RLS, não da ausência de GRANT; e (b) a **dormência do `service_role` (C-2 / D-09)**, que é premissa de segurança do M2, também é falsa. Nos dois casos a proteção real continua existindo (RLS deny-by-default), mas a **defesa em profundidade some**: passa a haver uma única camada onde o design conta com duas. **Verificação (rodar no SQL Editor do dashboard, é leitura pura):** `select table_name, grantee, string_agg(privilege_type, ',' order by privilege_type) from information_schema.role_table_grants where table_schema='public' and grantee in ('anon','authenticated','service_role') group by 1,2 order by 1,2;` — o esperado é o mesmo do local: `anon` só com `SELECT` em `book`/`genre`/`review` e NADA em `editor`/`comment`/`recommendation`; `service_role` sem DML em lugar nenhum. Divergiu → migration de REVOKE, na mesma passada de endurecimento da **TD-09** (que é o mesmo fenômeno visto pelo outro lado: `TRUNCATE` herdado de `ALTER DEFAULT PRIVILEGES`). **VERIFICADO em produção nesta reconciliação (2026-08-26), por leitura pura via `supabase db query --linked`: as 18 combinações tabela×papel batem exatamente com o esperado** — `anon` só com `SELECT` em `book`/`genre`/`review` e nada em `editor`/`comment`/`recommendation`; `service_role` sem DML em nenhuma tabela. **A-2 e a dormência do `service_role` (C-2/D-09) CONFIRMADOS no ar.** O defeito nunca chegou a produção — era exclusivo do banco EFÊMERO que o CI criava do zero, já corrigido no repositório (`auto_expose_new_tables = false`).
- [ ] **ACHADO — o CI é mais fiel que o ambiente local em uma variável** (2026-08-26). `listing.integration.test.ts` importa `review/queries` → `supabase/public` → `env.ts`, que valida `NEXT_PUBLIC_SUPABASE_*` no LOAD do módulo. A suíte NÃO declara essa dependência: localmente ela é satisfeita pelo `.env.local`, onde as duas variáveis apontam para o **projeto REMOTO de produção**. Ou seja: a suíte "local-only" só passava porque havia credenciais de produção no ambiente. No CI elas são apontadas ao stack efêmero. **Não é bug de teste nem de CI** — é acoplamento não declarado, achado justamente porque o CI é um ambiente limpo. Se um dia a suíte deixar de receber um client explícito em algum chamador, ela passaria a falar com produção sem que nada avisasse.

- [x] **RESOLVIDA (2026-08-26, `fix/public-review-polish`) — UI: bloco vinho na página de resenha (prod).** **A causa não era a cor nem a altura do placeholder: era a AUSÊNCIA de container.** A classe `lia-review` está no `<article>` da rota desde o M1, mas **nenhuma regra de CSS a selecionava** — não é cascata nem especificidade, o seletor nunca existiu. Sem `max-width`, o artigo herdava a largura inteira do `<main>` (1280px) e a capa, filha direta dele, herdava junto: `.lia-card__media` é `width:100%` + `aspect-ratio:3/2`, ou seja **não tem altura própria — tem a que a largura lhe der**. 1280 × 2/3 = **853px medidos em navegador** de `oxblood-700` sólido antes de qualquer conteúdo. Numa célula da grade da home (~350px) a mesma regra rende ~233px, e por isso o defeito só aparecia na rota de resenha. **Correção no CONTEXTO, não na regra de mídia:** `.lia-review` ganhou container (`--container-md`, centrado, com recuo) e a capa ganhou `.lia-review__cover` com largura ABSOLUTA (`min(11rem, 100%)`) + `aspect-ratio: 2/3` (proporção de LIVRO, não de banner). **853px → 264px** (176 × 264). Largura absoluta, e não percentual, é deliberado: uma porcentagem faria a capa reabrir junto com qualquer container mais largo — que foi exatamente como o defeito nasceu. `BookCover` passou a aceitar `coverUrl` e renderizar `<img>` de verdade quando existe (exibição pura de URL que a validação já restringe a http/https — o pipeline de UPLOAD segue sendo RVW-12); as duas variantes ocupam a MESMA caixa, então não haverá salto de layout quando as capas chegarem. Gate axe estrito (violations = 0 **e** incomplete = 0) nas **duas** variantes, em navegador.
- [x] **RESOLVIDA (2026-08-26, `fix/tags-separator`) — tags coladas em `/resenha/o-projeto-rosie`.** **A causa era de GRAVAÇÃO, não de exibição.** `listaPorVirgula` (`src/lib/review/schema.ts`) fazia `split(',')` — só vírgula. O editor separou por `;`, o parser não achou separador nenhum e a linha inteira virou UMA tag: `'neurodiversidade; autismo; amor; saúde mental; comédia romântica; vínculos humanos.'`. `ReviewTags` estava correto o tempo todo — renderiza um `<li>` por elemento, e o array tinha um elemento. **Correção:** a regra virou `SEPARADOR_DE_TERMOS = /[,;]/` e a função foi renomeada para **`listaDeTermos`** — o nome anterior citava a regra e virou mentira no dia em que a regra mudou; o nome novo descreve o PROPÓSITO e a regra mora numa constante greppável. Aceitar `;` **remove** um palpite (as duas pontuações são separadoras de lista sob qualquer leitura), ao contrário de aparar pontuação, que **acrescentaria** um — ver decisão abaixo. **Quebra de linha NÃO foi incluída** (uma lista colada uma-por-linha recai no mesmo defeito): fora do escopo, e o lugar de resolver é o mesmo parser. **O formulário passou a dizer a convenção**: os `helpText` de Tags e Palavras-chave agora dizem "Separe por vírgula ou ponto e vírgula" — corrigir o parser sem corrigir o texto deixaria de pé a metade adivinhatória do defeito. Os dois testes de regressão do `ReviewTags` foram **mantidos** (o atalho de dar `split` na exibição continua errado: mascararia defeito futuro de gravação e quebraria tag que legitimamente contenha `;`); só o comentário foi atualizado, que descrevia produção e virou histórico.
- [x] **DECISÃO REGISTRADA (2026-08-26) — pontuação no fim da tag NÃO é aparada.** O caso real trouxe `'vínculos humanos.'`: o editor terminou a lista como se termina uma frase. Aparar deixaria a tag limpa, e mesmo assim **não é o que o parser faz**. Razão: aceitar `;` além de `,` REMOVE um palpite; remover o ponto final ACRESCENTA um — o parser passaria a decidir que o editor "não quis" um caractere que digitou, e erraria em `etc.`, `S.A.`, `vol.`, `Jr.`. É o princípio já registrado em `formData.ts` (dado perdido é pior que dado não perguntado), agravado por ser perda SILENCIOSA. **Assimetria que decide:** o ponto sobrando é VISÍVEL na tela e o editor pode corrigi-lo; um caractere comido por regra invisível, não. O lugar de pegar isso é a UI — um eco das tags já separadas ao lado do campo —, não uma heurística de pontuação enterrada na validação. Prendido por teste (`etc.; S.A.; vol.` e `séc. XIX; J. R. R. Tolkien` sobrevivem inteiros).
- [ ] **ACHADO — duplicatas de tag passam inteiras, e produzem chave duplicada de React** (2026-08-26, comportamento HERDADO, não introduzido). `'amor; amor; Amor'` → `['amor','amor','Amor']`: sem dedup e sensível a maiúsculas/acentos. Vale igual para `keywords`, onde duplicatas viram `<meta keywords>` repetido. **Consequência já observável:** `ReviewTags` usa `key={tag}`, então duas tags iguais disparam "Encountered two children with the same key" (verificado). **Não mudado de propósito** — deduplicar exige decidir ANTES o que conta como repetida (maiúsculas? acentos? plural?), o que é decisão de produto, não linha de parser. Prendido por teste, para que a mudança futura seja deliberada. Se for deduplicar, o `key={tag}` do `ReviewTags` deixa de ser frágil junto.
- [ ] **ARGUMENTO PARA PRIORIZAR REV-19 (editar resenha) NO M4 — o dado de `o-projeto-rosie` foi corrigido por SQL em 2026-08-26.** As tags da resenha estavam gravadas como um elemento só; a correção do parser vale para gravações FUTURAS e não regrava o passado. **Não havia caminho pela interface:** a rota `/admin/resenhas/[id]/editar` (REV-19) está **diferida** desde o Execute do M3 (ver `tasks.md` §Diferidos), então a única saída foi `UPDATE` direto no banco de produção. **Isso é sintoma da lacuna, não solução.** Um editor que erra qualquer campo hoje depende de alguém com acesso ao banco — sem trilha de auditoria, sem RLS exercida (a policy own-or-admin da 0008 é justamente o que um `UPDATE` por SQL contorna), e sem `updated_at` coerente com quem de fato editou. Toda correção de conteúdo até o REV-19 chegar repetirá esse caminho. O RPC `update_review_with_book` e as policies de UPDATE **já existem** (T1/T2, criados e não exercidos): faltam `updateReview` (action), `getEditorReviewForEdit` (query) e a rota.
- [ ] **DECISÃO DE PRODUTO PENDENTE — atribuição da frase de destaque** (2026-08-26). O T11/T12 deixou o `figcaption` neutro ("Trecho em destaque") porque **não existe coluna de atribuição** — decisão correta e documentada em `HighlightQuote.tsx`. O uso real mostrou que o editor QUER atribuir: em `o-projeto-rosie` ele escreveu a fonte dentro do próprio campo — `"…uma grande vantagem." (O Projeto Rosie)`. **Recomendação: `highlight_source text NULL` (migration aditiva), não texto de ajuda.** Razão: com a fonte embutida no `highlight_quote`, o `figcaption` continua mentindo por omissão e a atribuição vira texto solto dentro do `blockquote` — que é justamente o lugar reservado à CITAÇÃO, não a quem a disse; além disso o dado fica impossível de exibir, indexar ou corrigir em separado. Com a coluna, o `figcaption` passa a nomear a fonte de verdade (`<cite>` volta a ser legítimo, porque aí há título de obra a citar) e a ausência degrada para o texto neutro de hoje. **Custo:** migration + RPC + schema + um campo no formulário — fora do escopo do polish, é uma fatia pequena de M4. **MITIGAÇÃO APLICADA em 2026-08-26** (`fix/tags-separator`): o `helpText` do campo passou a dizer "Não há campo de fonte: para atribuir, escreva a fonte no próprio texto" — o editor agora sabe o que está fazendo ANTES de digitar, em vez de descobrir depois de publicar. Não substitui a decisão sobre a coluna; só para de deixar o editor no escuro.
- [x] **ACHADO — bug histórico do `formatRating` (`,5 / 5` sem dígito inteiro) NÃO se reproduz mais** (verificado em produção, 2026-08-09): a home renderiza `4,5 / 5`, `4,0 / 5`, `5,0 / 5` corretamente. Resolvido por observação — sem investigação adicional.
- [ ] **ACHADO — seed de produção TEM notas com meio-ponto** (verificado em produção, 2026-08-09; relevante para M3/D-01): `dom-casmurro` = 4,5 e `iracema` = 4,5 (as outras duas resenhas são 4,0 e 5,0). Confirma empiricamente que a normalização da D-01 (inteiros 0–5) vai precisar tratar dados existentes na migration 0009. Fato registrado — a estratégia (round/floor/ceil) é decisão a tomar na revisão do Design de `reviews-crud`.

### Pendências de design a decidir (backlog — não bloqueiam telas públicas, resolver ANTES de aplicar o design)

- [ ] **Cores sálvia (`#4f5c47` e `#dce5d3`) — decisão pendente.** Aparecem no `docs/design/LIA Marca.html` mas **NÃO existem** em `lia-tokens.css` (o par verde dos tokens é semântico: `#1f6b3b`/`#e8f1ea`). Decidir: **adotar como acento novo da marca** (viram tokens nomeados no design system) **ou descartar**. **Resolver antes de a listagem (`review-listing-search`) consumir tokens**, para não espalhar cor hard-coded fora do sistema.
- [x] **RESOLVIDA (2026-08-26, T10): a rota é `/admin/resenhas/nova`.** O **wireframe** dizia `/admin/resenhas/novo` e o **sitemap** dizia `nova`; decidido **`nova`** por concordância com o substantivo — cria-se uma resenh**a**. A rota está no ar sob `(protected)` e o build a lista como `ƒ /admin/resenhas/nova`. `novo` **não** existe e **não** ganha redirect: nada apontava para ele (o wireframe é artefato de design, não código), então um redirect só criaria uma segunda URL para a mesma tela. **Corrigir o wireframe** quando o `docs/design/` for atualizado — a divergência agora é do artefato, não do código.

---

## Lessons Learned

- **Oráculo de verificação de RLS: leia o resultado por SUPERUSER (psql-postgres), NUNCA pelo mesmo role sob teste** (2026-07-08, descoberto na matriz T16 de `security-foundation`). Ao provar "editor A NÃO deletou/editou o recurso de B", a asserção de "linha intacta" precisa **ler a verdade do banco por um caminho privilegiado**. Ler via o próprio client sob RLS (ou via `service_role`, que **também** não tem GRANT nessas tabelas do M2 — 42501) devolve `null`/vazio **independentemente** de a operação ter sido negada → o teste passa por **falso-verde** (o oráculo confunde "negado" com "não-encontrado"). Aconteceu: a verificação de admin-delete "passou" antes de a linha ser realmente deletada. **Correção aplicada:** `psqlScalar()` roda `docker exec … psql -U postgres` (bypassa RLS e grants) para ler o estado real; as **ações** continuam pelos clients autenticados sob RLS. **Aplicar nas matrizes futuras** de `comment`/`recommendation` (M3): setup e verificação por `postgres`; ações e negações pelo role real. Corolário: `service_role` **não** serve de oráculo aqui (dormência C-2/D-09 — sem GRANT de tabela).
- **Testes que importam a cadeia de `queries.ts` (→ `supabase/server` → `env.ts`) devem usar import DINÂMICO dentro do setup (`beforeAll`)** (2026-07-06, descoberto no CI do PR #3). `env.ts` valida `process.env` com Zod **no load do módulo** — um import estático detona a validação no job vitest do CI (que não tem vars do Supabase) ANTES de o `describe.skipIf` pular a suíte: o arquivo falha inteiro com ZodError, mesmo com 100% dos testes passando/pulados. **Sorrateiro:** o gate local NÃO reproduz (o `.env.local` existe e o `vitest.config` o carrega via `loadEnv`) — local verde não prova nada aqui; a prova é o CI (ou rodar com `.env.local` renomeado). **Aplicar em qualquer teste futuro que toque `queries.ts`/`env.ts`:** `let queries: typeof import('../queries')` + `queries = await import('../queries')` no `beforeAll` (padrão do `rls.integration.test.ts`, que só importa `createClient` + tipos). NÃO "consertar" pondo placeholders de env no job de teste do CI — mascara o acoplamento.
- **Gate de código DEVE incluir `npm run format:check` (Prettier), não só typecheck+lint** (2026-07-05, descoberto no PR #2 da `review-page`). No Execute rodei `typecheck` e `eslint` por task, mas **não** o `format:check`; o CI (job `lint + format + types`) reprovou `prettier --check` em 3 arquivos (`page.tsx`, `queries.ts`, `rls.integration.test.ts`) — só formatação, corrigido com `prettier --write` (commit `a150297`). **Aplicar sempre:** o gate quick/full local deve ser `npm run typecheck && npm run lint && npm run format:check && npm test`, espelhando o job do CI. eslint e prettier são gates independentes.
- **ISBNs de exemplo nos specs têm checksum inválido** (2026-06-10, descoberto no T-11). Os ISBNs citados nas tasks/design (`9788535902775`, `8535902770`) e no seed (`9788520932051` para O Cortiço) **não passam** na validação de checksum — foram escritos sem calcular o dígito verificador. A implementação do `isbn.ts` é correta e os rejeita. **Aplicar em T-16 e T-21:** usar ISBNs com checksum verificado (ex.: `9783161484100`, `0306406152`, `080442957X`) ou recalcular o dígito verificador dos ISBNs reais antes de usá-los no seed/schema; não copiar os exemplos do spec verbatim.

---

## Quick Tasks Completed

| #   | Description | Date | Commit | Status |
| --- | ----------- | ---- | ------ | ------ |
| —   | —           | —    | —      | —      |

---

## Deferred Ideas

Itens fora de escopo do MVP, preservados para fases futuras (não bloquear o modelo de dados):

- [ ] `sections-extra` — listas, desafios, clubes do livro, enquetes — Captured during: PRD
- [ ] `analytics-reports` — resenhas mais vistas, comentários por período, buscas populares — Captured during: PRD
- [ ] `monitoring` — Sentry (erros) + Vercel Analytics ou Plausible (métricas) — Captured during: PRD

---

## Todos

Decisões em aberto a resolver na feature correspondente (ver [DECISIONS.md](DECISIONS.md)). Reorganizado por milestone em 2026-08-26 (o registro cronológico único tinha crescido a ponto de esconder o estado atual atrás de ~100 itens) — nenhum item foi apagado ou reescrito, só reagrupado; a ordem DENTRO de cada bloco preserva a ordem cronológica original.

### M0 — infra-foundation

- [x] Especificar a feature `infra-foundation` (M0) — spec.md criado
- [x] Versão do Tailwind confirmada: **v4 (`@theme`)** — ADR D-07 (alimenta INFRA-07)
- [x] Desenhar a feature `infra-foundation` (M0) — design.md criado
- [x] Fase Tasks de `infra-foundation` concluída — 10 tasks, 17/17 reqs mapeados
- [x] T-01 → T-09: implementação de `infra-foundation` concluída
- [x] T-10: migration aplicada no Supabase remoto (deploy M0 done)
- [x] M0 `infra-foundation` **concluído** — CI verde, RLS deny-by-default ativo na nuvem

### M1 — book-data · review-page · review-listing-search

- [x] D-04 (busca) — **RESOLVIDA (Aceita, 2026-07-06)**: server-side no Supabase via `ilike` sobre `review.title` (C-1), full-text/pg_trgm adiado para quando o volume justificar. ADR completa em DECISIONS.md
- [ ] Handoff M1: adicionar RLS policies de leitura (`status='published'`) — M0 entrega RLS deny-by-default
- [x] Iniciar M1 pela feature `book-data` (ficha técnica) — spec.md criado
- [ ] book-data: decisões do Specify registradas — ISBN **opcional, validado se presente** (checksum ISBN-10/13, armazenado normalizado, exibido formatado); seed popula **só livros + gêneros** (resenhas nas features de resenha); `genre_id` endurecido para NOT NULL
- [x] Revisar spec de `book-data` — aprovado; ajuste: RLS de **leitura pública (SELECT) de `book`** entra nesta feature (BOOK-17), escrita fica no M2; `cover_url` confirmado como referência textual (imagem em `storage-covers`)
- [x] Desenhar a feature `book-data` (design.md) — 12 componentes, 8 decisões (DD-1..8); 17/17 reqs endereçados
- [x] Revisar design de `book-data` — aprovado (DD-1..8 ok, TD-02 registrado)
- [x] Fase Tasks de `book-data` concluída — 12 tasks (T-11..T-22), 17/17 reqs mapeados
- [x] **Execute `book-data` concluído** — 12/12 tasks, 7 fases; build/typecheck/test/axe/Lighthouse verdes; RLS local 4/4. Pronto para PR (branch `feat/book-data`)
- [ ] Handoff M1: RLS de leitura de `review` (`status='published'`) — **endereçado na spec de `review-page`** (RVW-13/14/15: policy filtrada + GRANT TD-03); a implementar no Execute
- [x] Especificar a feature `review-page` (M1) — spec.md + context.md criados; gray areas C-1 (nota só número) e C-2 (placeholders "em breve") resolvidas; **aguardando revisão antes do Design**
- [x] Desenhar a feature `review-page` (design.md) — **aprovado** 2026-06-12; 27/27 reqs mapeados a componentes; 3 pontos da revisão resolvidos (draft via 5º book; `<h2>Resenha`; `metadataBase`)
- [x] Fase Tasks de `review-page` concluída — **9 tasks (T-23..T-31), 27/27 reqs mapeados**, alocação de modelo definida; `tasks.md` criado
- [x] Execute `review-page` — **10/10 tasks (T-23..T-32) implementadas e commitadas**; gates de código verdes (typecheck/build/lint + 93 passed/8 skipped). Verificação local Supabase (T-23/24/31/32) pendente (TD-02)
- [x] Ajuste pré-Execute `review-page` — T-26 dividida (formatRating util + Rating componente), renumeração T-27..T-32, dependency graph corrigido (T-23→T-24 sequencial); código realinhado (`rating.ts`→`formatRating.ts`, commit `7f0d925`)
- [x] `review-page`: branch pushada para o origin (CI rodando no push)
- [x] `review-page`: **PR #2 aberto** para `main` (https://github.com/gabriel1henrique1rocha-crypto/lia/pull/2) via `gh` (instalado 2.96.0 + autenticado). CI **verde** após fix de Prettier (`a150297`): lint+format+types, vitest (93/8), axe+lighthouse (`/`+`/styleguide`), Vercel. **Sem merge — aberto para revisão.**
- [ ] `review-page`: rodar verificação local dos gates de banco (`supabase start && db reset`; axe da rota `/resenha/[slug]`; `RUN_RLS_INTEGRATION=1`)
- [ ] **TD-03 (Alta, pré-M2):** a migration 0005 (T-23) concede GRANT só a `review`; abrir frente de infra para GRANTs de `comment`/`recommendation`/`editor` + `service_role`/Data API **antes do M2 (`reviews-crud`)**
- [x] **Lacuna de design RESOLVIDA** (2026-07-05, commit `45b90c4`) — artefatos versionados em `docs/design/`: `LIA Marca.html` (design system/marca), `Wireframes LIA (standalone).html` (wireframe lo-fi de 4 telas + estados), `LIA — Sitemap da aplicação.pdf` (rotas públicas e admin). Cobertura confirmada: wireframe cobre a **página de resenha individual (Tela 2)** E a **listagem/home (Tela 1)**. Design system é **evolução coerente** com `lia-tokens.css`/`lia-components.css` (mesma paleta e tipografia, sem conflito estrutural; só as 2 cores sálvia divergem — ver backlog)
- [x] **Especificar `review-listing-search` (M1)** — spec.md (27 reqs `LST-01..27`) + context.md **APROVADOS** (2026-07-06): C-1 busca só em `review.title`, C-2 header mínimo semântico, C-4 `/genero/[slug]` adiado, D-04 server-side `ilike` (Aceita). Escopo: **entra** home `/` (grid, busca, filtros gênero/autor/nota, ordenação, paginação, estado vazio, carrossel manual, `ReviewCard` compartilhado, leitura anon 0004/0005 — sem migration); **fora** `/genero/[slug]`, `/recomendacoes` (M3), toggle grid⇄lista, full-text
- [x] **Desenhar `review-listing-search` (design.md)** — criado 2026-07-06: 10 decisões DD-1..10, **27/27 reqs mapeados**, 6 componentes novos (`ReviewCard`, `ListingControls`, `Pagination`, `EmptyState`, `FeaturedCarousel`, `SiteFooter`) + reuso (BookCover/Rating/excerpt extraído), query única `ilike`+filtros+`range`/`count`, SEO canonical `/`+noindex p/ params, sálvia estreia na seção destaque (DD-7). **Aguardando revisão antes de Tasks**
- [x] **`review-listing-search` — verificação de design + fase Tasks** (2026-07-06): 3 pontos verificados no design.md — **(1) aria-live: COBERTO** (DD-9, reforçado com a mecânica explícita de live region em navegação SSR); **(2) FeaturedCarousel: SEM auto-rotação** (DD-5/§4/LST-16 — nenhum gate WCAG 2.2.2 a resolver, sem parada de decisão); **(3) escapeLike: era VAGO → tornado EXPLÍCITO** (§3: ordem `\`→`\\` primeiro, depois `%`/`_`; + flag de verificação supabase-js/PostgREST na impl). **tasks.md criado** — 14 tasks (T1..T14), **27/27 reqs mapeados sem órfãos**, 3 gates de validação ✅, alocação de modelo por task, leitura anon/sem migration (TD-04/LST-19) registrada. **Aguardando revisão antes de Execute.**
- [x] **Sálvia adotada** (Execute): tokens `--color-sage-100` (#dce5d3) / `--color-sage-700` (#4f5c47) criados no `@theme` (não existiam no repo). Fecha a pendência "cores sálvia".
- [x] **Execute `review-listing-search` CONCLUÍDO** (2026-07-06, branch `feat/review-listing-search`, 19 commits, **sem PR/deploy** — aguardando revisão): 14 tasks (T1–T14) + fix prettierignore + hardening da home + migration 0006 + fix PGRST103. Gates de código VERDES em cada commit (typecheck/lint/format/**155 passed** / 16 skipped). Build ok; `/` agora **dinâmica (ƒ)**. **axe do CI provado local** (`/`+`/styleguide` 0 críticos, contra dados reais e contra o fallback). **Home verificada end-to-end** contra Supabase **local** (4 publicadas, draft ausente, busca `q=dom`→1, filtros de gênero derivados, `pagina=999` normaliza sem 500, robots noindex com params). **Integração T4 8/8** local (anon só lê published; escape %/_→0).
- [x] **`review-listing-search` — PR #3 aberto, CI verde e MERGEADO em `main`** (`b127316`). Revisão do diff conduzida (0006, escapeLike, home resiliente); fix do CI (import dinâmico no teste de integração — ver Lessons). Branch pushada, `main` local sincronizado.
- [x] **0006 APLICADA em produção** (2026-07-07): `supabase db push` executado no projeto LIA (`gcfsiaxyvfmoyasxjflx`); policy `genre_public_read` confirmada em `pg_policies`; efeitos validados no ar — filtro de gênero na home + gênero de volta na ficha `/resenha/dom-casmurro`. **Não há push pendente — NÃO reaplicar.**
- [x] **ACHADO (lacuna pré-existente) — RESOLVIDO:** `genre` tinha RLS habilitada (0001) + GRANT (0004) mas **nenhuma policy de SELECT** (0003 só cobriu `book`) → anon lia 0 gêneros; afetava a **review-page em produção** (ficha com gênero **null/omitido**). Corrigido pela **0006** (exceção pontual ao "sem migration" do LST-19), **aplicada em produção 2026-07-07** — ficha com gênero de volta, validada.

### M2 — security-foundation

- [x] **`security-foundation` (Specify) — decisões da revisão APLICADAS** (2026-07-07): spec.md (**SEC-01..19**) + context.md (**C-1..C-6 RESOLVIDAS**). **C-1** magic link (sem senha); **C-2** escrita padrão = client autenticado (JWT) sob RLS, `service_role` só exceção documentada e **dormente** nesta feature (exige policies de escrita p/ o papel autenticado); **C-3** `server-only`+env+lint boundary; **C-4** bootstrap manual do 1º admin (documentado, sem segredo no repo); **C-5** `admin-auth-editors` **fundida** aqui (auth de editor é parte da fundação); **C-6** moderação de `comment` FORA — só garante/testa deny-by-default. Modelo de auth de editor explícito no spec (editor.id = auth.users.id; papel via `editor.role`/`active`; RBAC no app E na RLS). Possível ADR **D-09** (modelo de escrita). **Aguardando revisão antes do Design.**
- [x] **Desenhar `security-foundation` (design.md)** — criado 2026-07-07 (§1–§9, DD-1..19, 19/19 reqs rastreados): 3 clients (`public.ts`/`authenticated.ts`/`admin.ts`; `server.ts`+`client.ts` REMOVIDOS — fallback `??` morre), env dividida por client (`env.admin.ts` lazy), isolamento em 3 camadas (`server-only` + env sem `NEXT_PUBLIC` + lint allowlist VAZIA = dormência C-2 como código), `editor` da 0001 JÁ conforme (sem ALTER), migrations previstas **0007** (RBAC editor + `is_admin`/`is_active_editor` security definer — anti-recursão 42P17) e **0008** (review own-or-admin) NÃO aplicadas, magic link token_hash via `@supabase/ssr ^0.12.0` (a instalar) com `shouldCreateUser:false`, gate = layout `(protected)`+`requireEditor()` autoritativo + `proxy.ts` só refresh (CVE-2025-29927), threat model 13 modos (F-1..F-13), regressão T-a..T-f. §9: A-1..A-11; **A-2/A-4 aprovados com os defaults do design** na revisão.
- [x] **Fase Tasks de `security-foundation` concluída** (2026-07-07) — **19 tasks (T1..T19), 19/19 reqs mapeados sem órfãos**, 3 gates de validação ✅ (granularidade, diagrama×deps, co-locação de testes), alocação de modelo por task. STOPs explícitos: `db push` produção FORA das tasks (pós-merge, humano); `SUPABASE_SERVICE_ROLE_KEY` não entra na Vercel (SEC-17); SMTP/template = runbook (T17). **Aguardando revisão antes do Execute.**
- [x] **Execute `security-foundation` T1–T18** (2026-07-08, branch `feat/security-foundation`, sem push): fundação de clients (T1–T7), fluxo de auth magic link (T8–T11), migrations 0007/0008 local (T12–T13), provas SEC-14/build-boundary/matriz-RLS-17-17 (T14–T16), runbook + D-09/D-10 + TD-04 fechada/TD-03 reduzida (T17–T18). Gate verde por commit; revisões por grupo aprovadas (Fundação/Auth/Migrations/Provas). Defaults A-2 (anon sem GRANT; deny=vazio OU 42501) e A-4 (review own-or-admin, editor publica own) aprovados. Decisões da sessão: callback token_hash (não PKCE, cross-device), D-10 (sessão server-only + cookies httpOnly nos 3 pontos de escrita), contrato anti-recursão da 0007 (self direto + admin via definer, editor NO FORCE).
- [x] **T19 — verificação end-to-end (Mailpit local) VERDE 11/11** (2026-07-08): `scripts/e2e-magic-link.mjs` dirigiu o fluxo real (login → magic link no Mailpit → `/auth/confirm` token_hash → sessão → gate `/admin`). **D-10 confirmado empiricamente**: todos os cookies de auth são **HttpOnly + SameSite=Lax** (A-10 fechado). Gate: `/admin` com sessão → 200 (papel admin resolvido); sem sessão → 307 `/admin/login`; refresh do proxy sobrevive. Bootstrap do admin de teste pelo caminho privilegiado (GoTrue admin + psql), não service_role.
- [x] **PR #4 aberto para `security-foundation`** (2026-07-08). Ordem de rollout aplicada (refinamento A-11, PRÉ-merge): 0007/0008 aplicadas em produção via `db push` a partir do checkout da branch (**9 policies confirmadas em `pg_policies`**: 4 editor + 5 review). Pós-merge pendente: SMTP/template → bootstrap do 1º admin (SQL privilegiado, não service_role) → verificar login (ver [runbook §5](../../docs/runbook-admin-bootstrap.md)). Gate SEC-17: NÃO configurar `SUPABASE_SERVICE_ROLE_KEY` na Vercel. **Gatilho TD-02:** mover as suítes RLS/e2e para os required checks do CI antes do 2º editor real.

### M3 — reviews-crud

- [x] D-01 (escala da nota) — **RESOLVIDA (Aceita, 2026-08-09)**: escala inteira 0–5, sem meio-ponto (elimina a granularidade de meia-estrela do PRD). ADR completa em DECISIONS.md
- [ ] D-02 (anti-spam) — resolver em `public-comments` (M3)
- [ ] D-03 (modelo de indicação) — resolver em `recommendations` (M3)
- [x] **`reviews-crud` (Specify) — CONCLUÍDO** (spec.md + context.md): **25 reqs `REV-01..24` + REV-07-schema**; **D-01 Aceita** (nota inteira 0–5, sem meio-ponto — ADR em [DECISIONS.md](DECISIONS.md)). Gray areas de produto resolvidas: **CAPA=a** (`cover_url` textual agora; upload ao Storage → `storage-covers`), **TAGS=c** (guardar/exibir agora; filtro na home adiado), **RESENHISTA=b**, **PAINEL=a** (lista mínima `/admin/resenhas`). Achado técnico: a 0008 provisionou escrita só de `review` — **a 0009 precisa abrir a escrita de `book`** (GRANT+policies own-or-admin).
- [x] **`reviews-crud` (Design) — CONCLUÍDO e APROVADO com emendas** (revisão humana 2026-08-09): design.md com **DD-1..18 e REV-01..24 + REV-07-schema todos mapeados** (§10). Mudança desde o Specify: resenhista **DENORMALIZADO** em `review.reviewer_name` (congelado por resenha, lido sob self-read RLS — sem tocar a RLS de `editor`). Decisões estruturais: posse de `book` **own-or-admin transitiva** via review 1—1; **atomicidade book+review por RPC `SECURITY INVOKER`** numa transação PostgREST (DEFINER rejeitado — RLS continua sendo o gate); slug **estável no edit** (definer `unique_review_slug` + UNIQUE backstop); nota inteira = **CHECK no banco + Zod**. **Emendas da revisão:** **A-1** confirmado *com emenda* — o schema de validação passa a ser derivado do **status validado como enum**, não do botão clicado (fecha publicação incompleta por `FormData` forjada pelo caminho normal do app; residual encolhe para API direta); **A-3 RESOLVIDO por verificação empírica** — normalização da nota legada por **UPDATE editorial explícito por slug** + rede residual genérica (§2.2 do design; estratégia vive só lá); **A-7** confirmado **sem emenda** (exibição pública cabe em `reviews-crud`); **A-8** confirmado *com esclarecimento* — `published_at` é carimbo da **primeira** publicação, republicar não reescreve (não empurra a resenha para o topo de "Mais recentes"). Menores: milestone corrigido para M3, `search_path = ''` padronizado nas funções da 0009, `revalidatePath` **também no unpublish**, e o **oráculo de slug** nomeado como residual explícito (sufixo `-2` revela existência de draft alheio — aceito). **CORTE DE ESCOPO POR PRAZO** (§13, decisões e não omissões): a **0009 permanece ÍNTEGRA**; sai só a superfície de UI — `further_reading` fora da UI/exibição (`RepeatableLinks` **não será construído**), tags/keywords viram **um input separado por vírgula** (sem chips dinâmicos), e `update_review_with_book` + rota `/admin/resenhas/[id]/editar` **fora do Execute** (RPC e policies ficam na 0009, apenas não exercidos). Mantidos: `reviewer_name`, `highlight_quote`, `publication_city`, nota inteira, publish/unpublish e a exibição pública dos campos mantidos. Consequência registrada: a defesa XSS de URL fica exercida só por `cover_url` — **filtro http/https mantido mesmo assim**. **Próxima fase: Tasks (não iniciada).**
- [x] **`reviews-crud` — fase Tasks concluída; `tasks.md` (T1..T13, diferidos, plano de corte) mergeado em `main` via PR #7.** Execute iniciado na branch `feat/reviews-crud`, rebaseada sobre `main` pós-merge de PR #7/#8 (SHAs reescritos pelo GitHub: `d01f542`→`536d849`, `158b15b`→`5168bd0`). **2026-08-24 — reconciliação pós-merge:**
  - T1 pt.1 da 0009 **commitada** (`49a47bf`): colunas aditivas, `CHECK review_rating_integer`, GRANTs de `book` a `authenticated`, helper `owns_book_via_review`, policies de `book`. Seed corrigido em seguida (`6b1edd9`) para notas inteiras. **T1 é Parcial** — 6 dos 9 "Done when" seguem `[ ]` (ver bloco "PENDÊNCIAS DE FECHAMENTO" no próprio `tasks.md`)
  - **0009 pt.2 NÃO escrita** — RPCs `create_review_with_book`/`update_review_with_book`, helper `unique_review_slug`, regen de `database.types.ts`: é o T2, ainda não iniciado
  - **0009 NÃO aplicada em produção**; o registro de migração mais recente em produção continua sendo a `0008`. **REGRA: não fazer `db push` da 0009 antes de a pt.2 estar escrita no arquivo**
  - Calendário 13–15/08/2026 **não foi executado**; o Plano de Corte do `tasks.md` está **suspenso** (nota registrada no próprio arquivo, gatilhos preservados sem alteração) — escopo integral mantido até um prazo externo reaparecer com nova âncora
  - **Nova arquitetura de informação pública entregue e mergeada (PR #8):** header com 6 destinos (`SiteNav`), 5 páginas placeholder honestas (`/quem-somos`, `/autores`, `/filmografia`, `/liacast`, `/sugestoes`), "Resenhas" renomeado para **"Catálogo"** apontando para `/` (não existe rota `/catalogo`/`/resenhas` própria — ver TD-06)
- [x] **D-11 — remoção da nota (rating) do produto: FORMALIZADA e ACEITA (2026-08-24).** ADR completa em [DECISIONS.md](DECISIONS.md); **supersede D-01** (marcada Superseded, texto preservado). **Executado nesta data:** passo 1 da ORDEM DE REMOÇÃO — a `0009` perdeu a normalização editorial e a constraint `review_rating_integer` (`accdc9a`), reverificada com `db reset` local 2× (`EXIT=0`; constraint ausente, coluna `rating` presente e dormente, policies de `book` e GRANTs intactos); e a emenda das specs do M3 (`d9d137c`) — T9 REMOVIDA sem renumerar, T5/T6/T8 encolhidas, REV-07 extinto. **Pendente:** passo 3 (parar de ler/filtrar/ordenar) bloqueado por D-12; passo 4 (drop da coluna) sem data. **Achado que exige decisão:** T4 perdeu o injetor de falha (usava `p_rating` fora de faixa contra o CHECK) — candidatos registrados na task, escolha é de design. Registro original da pendência: Impacto: a 0009 perde o `CHECK review_rating_integer` e a normalização de dado legado; T9 (`RatingInput`) deixa de existir; T5/T8/T11/T12 encolhem (menos campo a validar/renderizar); o M1 em produção (que já exibe nota) precisa de regressão. A coluna `review.rating` **NÃO deve ser dropada** nesta etapa — só a decisão de produto, sem migração de remoção ainda. A remoção do filtro por nota na home só pode ocorrer **DEPOIS** que o filtro por deficiência representada (D-12) existir, sob pena de deixar a home sem filtro algum.
- [x] **D-12 — taxonomia de deficiência representada: FORMALIZADA e ACEITA (2026-08-24), M4.** ADR completa em [DECISIONS.md](DECISIONS.md). Vocabulário **único e controlado** (`disability_term`; só admin cria/renomeia/desativa, nunca deleta) + junções por vertical (`review_disability`, `film_disability`). Rejeitada a alternativa de termos separados por vertical — argumento decisivo é a **assimetria de reversibilidade** (único→separado é particionamento; separado→único exige deduplicação com julgamento humano). **Bloqueia o passo 3 de D-11.** **Vocabulário inicial segue `[PREENCHER]`** — decisão editorial pendente, trava só a migration de seed, não a modelagem. Registro original da pendência: Vocabulário único e controlado (`disability_term`, só admin cria/renomeia/desativa) + junções separadas por vertical (`review_disability`, `film_disability`). Filmografia contemplada no Specify, implementada depois. Vocabulário inicial **[PREENCHER]**.
- [x] **EMENDA A D-11 (2026-08-24) — ORDEM DE REMOÇÃO COLAPSADA; COLUNA `review.rating` DROPADA.** Por decisão do responsável, os passos 2, 3 e 4 da ORDEM DE REMOÇÃO executaram **juntos**, em vez de aguardar D-12. **Executado:** (a) migration **`0010_drop_review_rating.sql`** criada — `drop column if exists rating`, em **arquivo novo**, sem reeditar a 0009; dependências verificadas sem necessidade de `cascade` (a única constraint era `review_rating_check`, inline de coluna da 0001, que cai junto; zero índices, views, policies ou funções dependentes; prova direta por `begin; alter table … drop column rating; rollback;` bem-sucedido). (b) **Regressão do M1 executada**: filtro por nota, ordenação "Melhor nota", param `nota`, select de nota, eco do estado vazio e toda a exibição removidos; `Rating.tsx` e `formatRating.ts` **deletados** com seus testes. (c) Seed sem `rating` e com o comentário falso sobre o CHECK corrigido. (d) `database.types.ts` regenerado — `rating` some e as colunas da 0009 entram (o arquivo estava defasado; exigiu acrescentar `publication_city` a 5 fixtures de `BookView`). **Verificação:** `db reset` local 2× `EXIT=0`, coluna ausente do schema, seed carregando 5 resenhas; `typecheck`/`lint`/`format`/**231 testes**/`build`/**axe 20/20** verdes. **⚠️ `0010` NÃO aplicada em produção — aguarda `db push` humano (A-11).** **IRREVERSÍVEL:** aplicada, as notas de produção só voltam por restauração de backup.
- [ ] **ESTADO CONHECIDO E TEMPORÁRIO — home sem filtro por nota.** A home perdeu o filtro por nota mínima e a ordenação "Melhor nota". **Correção factual ao registro anterior:** ela **NÃO** ficou "sem filtro algum" — seguem ativos a **busca por título**, o **filtro de gênero**, o **filtro de autor** e a **ordenação** (`recentes` default + `titulo`). A ordenação default **não precisou mudar**: já era `recentes` (`published_at` desc), nunca foi nota. O que falta é o eixo editorial do observatório — filtro por **deficiência representada** (D-12) —, bloqueado pelo vocabulário inicial `[PREENCHER]`. URLs legadas com `?nota=N` ou `?ordem=nota` **degradam sem erro** (param desconhecido é ignorado; ordem inválida cai no default), com teste cobrindo.
- [x] **T2 CONCLUÍDA (2026-08-25) — RPCs atômicos de submissão livro+resenha, migration `0011_reviews_crud_rpcs.sql`.** Criados `create_review_with_book`, `update_review_with_book` (ambos **SECURITY INVOKER**) e `unique_review_slug` (**DEFINER** mínimo e isolado), todos com `search_path = ''` e `GRANT EXECUTE` só a `authenticated` (`anon` ausente do `proacl`). **Arquivo NOVO, não "0009 parte B":** a 0009 e a 0010 já estão em produção e migration aplicada não se reescreve. **Provado localmente:** atomicidade (falha no INSERT de `review` após o de `book` → rollback, zero órfão, oráculo superuser); isolamento RLS (RPC sempre usa `auth.uid()`; INSERT forjando editor alheio negado pela policy; editor B não edita resenha de A → 42501 com a linha intacta; `anon` → permission denied); slug com sufixo `-2`/`-3` e fallback para base vazia. **Desvios registrados:** `unique_review_slug` é `volatile` (não `stable` como no design) porque toma lock consultivo; a garantia anti-corrida vale sob READ COMMITTED e o UNIQUE segue como backstop. **Bug encontrado e corrigido durante a verificação:** `pg_catalog.coalesce(...)` é erro de sintaxe — `coalesce`/`nullif` são construções do parser, não funções de schema (e por isso imunes a shadowing). **⚠️ `0011` NÃO aplicada em produção — aguarda `db push` humano (A-11).**
- [x] **T4 destravada:** o injetor de falha que D-11 havia matado (`p_rating` fora de faixa) tem substituto exercitado — `p_further_reading` não-array violando `review_further_reading_is_array`.
- [x] **T3 CONCLUÍDA (2026-08-25) — matriz COMPORTAMENTAL das policies de `book`** (`src/lib/supabase/__tests__/book-rbac-matrix.integration.test.ts`, 12 células, local-only). Até aqui as três policies da 0009 tinham sido verificadas só por EXISTÊNCIA no `pg_policies`; como o T2 delega a elas todo o isolamento entre editores, existir no catálogo não bastava. Operações via API com sessões reais; **todo estado lido por `psql -U postgres`**, nunca pelo papel sob teste. **12/12 verdes.** Provado: INSERT permitido a editor ativo e negado a `anon` (por GRANT, antes da policy); UPDATE permitido no book da própria review, negado no de outro editor e negado no órfão; UPDATE de admin permitido; DELETE admin-only (editor negado até no próprio book); SELECT de `anon` intacto (`book_public_read` da 0003); e o caminho real do RPC — o autor edita o book que criou, outro editor não. **PROVA DE VERMELHIDÃO:** afrouxando `book_editor_update` para `using(true)` **no banco local** (revertido por `db reset`, nenhuma migração tocada) a suíte reprovou em exatamente as 3 células que dependem dela (`3 failed | 9 passed`) — a suíte é load-bearing, não falso-verde.
- [ ] **ACHADO DE COMPORTAMENTO — `book` órfão é IMUTÁVEL para não-admin.** `owns_book_via_review` não acha dono para um book sem review vinculada, então `book_editor_update` nega para qualquer editor, **inclusive quem o criou** (`book_editor_insert` permite criar). Pelo caminho de produção isso não acontece — o RPC cria book+review na mesma transação —, mas um book inserido direto pela API fica publicamente legível, não-editável pelo autor e removível só por admin. **Não é bug de segurança** (falha fechado), e nenhuma correção foi feita: mudar exigiria migration nova e é decisão de arquitetura.
- [ ] **VEREDITO TD-02 (2026-08-25): o CI NÃO consegue rodar as suítes de RLS.** `.github/workflows/ci.yml` job `test` = `ubuntu-latest` + `npm ci` + `npm test`, **sem `services:`, sem stack Supabase, sem o container do oráculo e sem `RUN_RLS_INTEGRATION`**. Nenhum workaround foi tentado e **nenhum mock de RLS foi escrito** — mock de policy não prova policy. O gatilho da TD-02 (subir Supabase no CI **antes do 2º editor real**) segue de pé e agora cobre também a matriz de `book`.
- [x] **T4 CONCLUÍDA (2026-08-25) — atomicidade de `create_review_with_book` e `update_review_with_book`** (`src/lib/supabase/__tests__/create-review-rollback.integration.test.ts`, 4 casos, local-only). Injetor definido: `p_further_reading` não-array contra o CHECK `review_further_reading_is_array`, que dispara no INSERT/UPDATE de `review` **depois** do de `book` — a janela exata do órfão. **4/4 verdes.** Create sob falha: rollback total, zero órfão. Create OK: as duas linhas persistem ligadas, com `editor_id` = o chamador. Update sob falha: snapshot **campo a campo** de `book` e `review` idêntico (contagem não bastaria — o risco é a ficha do livro mudar com a resenha intacta, já que o RPC atualiza `book` primeiro). Update OK: as duas mudam juntas. **PROVA DE VERMELHIDÃO:** os RPCs substituídos **no banco local** por versões que engolem a exceção num subbloco (o `book` sobrevive e a transação commita) → `2 failed | 2 passed`, com os casos negativos reprovando e os positivos verdes; oráculo mostrou `orfaos 0 → 1` e o RPC devolvendo `null` **sem erro**. Revertido por `db reset`; **a 0011 não foi tocada**.
- [ ] **BUG ENCONTRADO NO PRÓPRIO TESTE, corrigido (2026-08-25) — asserção global sob paralelismo.** A 1ª versão da suíte T4 afirmava contagens GLOBAIS (`count(*) from book`) e passava sozinha, mas reprovava com `expected 18 to be 5` ao rodar junto com a matriz de `book` (T3): o Vitest roda arquivos em **paralelo** e as suítes integration criam/apagam linhas ao mesmo tempo. Corrigido escopando **toda** asserção por uma marca única de corrida, e acrescentando timeout explícito de 20s aos casos que fazem várias leituras de oráculo (cada `psqlScalar` é um `docker exec`, e 5s de default estouram sob carga). **Lição para as próximas suítes integration: nunca asserte contagem global.** Todas as 15 suítes integration passam juntas: `131 passed | 1 skipped`.
- [ ] **VEREDITO TD-02 reforçado (T4): nenhum mock de transação foi escrito.** Mock de transação prova ainda menos que mock de policy — a atomicidade **é** a transação, então simulá-la é afirmar a conclusão em vez de testá-la.
- [x] **T5 CONCLUÍDA (2026-08-25) — contrato de validação** (`src/lib/review/schema.ts`, `src/lib/review/slug.ts`, +50 testes; suíte em `281 passed | 52 skipped`). Espelhamento conferido contra as migrations **e** contra `pg_constraint`/`information_schema` do banco real, com um teste por constraint. `year` e `coverUrl` ficaram MAIS estritos que o banco de propósito (estrito nunca vira 500). A derivação de `review.title` (vazio → título do livro) vive no schema, não na UI, porque a coluna é NOT NULL e uma submissão fora da UI mandaria null ao RPC. **Fricção de tipos do T3 resolvida:** `toCreateReviewRpcArgs()` devolve o tipo que o `.rpc()` exige, então o T6 não precisa de cast; internamente usa `CreateReviewRpcArgs`, derivado do gerado, corrigindo nullability só nos params de coluna nullable. Alarme verificado: renomear um parâmetro quebra o typecheck.
- [x] **ACHADO DE SEGURANÇA FECHADO (2026-08-25, commit `fix(validation)`) — `bookInputSchema` NÃO cumpria A-4.** O `z.string().url()` do Zod 4 aceita `javascript:`, `data:`, `ftp:` e `vbscript:` (verificado), e `cover_url` é renderizado em `src`/`href` — vetor de XSS. A coluna **não tem CHECK no banco**, então a validação da app É o gate. Corrigido na ORIGEM (a ficha), não só no caminho de resenha; testes rejeitando os quatro esquemas com asserção de erro no campo. O override em `review/schema.ts` virou redundante e está declarado como defesa em profundidade — o comentário que dizia que a ficha aceita `javascript:` foi corrigido junto, para não virar armadilha.
- [x] **T6 CONCLUÍDA (2026-08-25) — server actions de resenha** (`src/app/admin/(protected)/resenhas/actions.ts`, +31 testes; suíte em `317 passed | 52 skipped`). `createReview`, `publishReview`, `unpublishReview`, todas com `requireEditor()` por operação (o layout protege páginas; server action é endpoint próprio). **Gate de publicação schema-determinístico (A-1) PROVADO POR VERMELHIDÃO:** trocando a derivação por "schema vem do botão" — a versão vulnerável, aplicada localmente e revertida — o teste do payload forjado reprova com `expected 'saved' to be 'error'`, isto é, `status=published` sem corpo **teria publicado**. **42501 não vaza existência:** linha invisível, 0 linhas afetadas e 42501 do banco produzem estado idêntico (`toEqual`). `23505` vira erro no campo `reviewTitle`; `23514` é logado como divergência de espelhamento. `published_at` só carimba na primeira publicação; `unpublish` não o limpa. **Zero casts de tipo** — a garantia do T5 se confirmou, e dois casts escritos por reflexo foram removidos após verificar que eram desnecessários.
- [ ] **REVALIDAÇÃO — escolha registrada:** só `/` e `/resenha/<slug>`. `/admin/*` **não** é invalidada: é `ƒ` por ler `cookies()` (confirmado no `next build`), então invalidar seria ruído. Rascunho e erro não invalidam nada. Nota: `/` e `/resenha/[slug]` também são `ƒ`, então o `revalidatePath` não é o único mecanismo de frescor — segue necessário por limpar o **Router Cache do cliente**.
- [ ] **LACUNA DE CONTRATO (2026-08-25) — 4 campos de `book` que o RPC não persiste.** `pages`, `original_language`, `translator` e `translated_from` existem na tabela (0001) e no `bookInputSchema`, mas **não estão na assinatura de `create_review_with_book`/`update_review_with_book`** (0011). O mapeador do T5 os descarta, com teste fixando o descarte. Se o formulário (T8) vier a coletá-los, o valor **não chega ao banco** — a correção seria acrescentar os parâmetros ao RPC por migration nova. O design (§6.3) não os inclui no formulário, então hoje não há regressão; fica registrado para não virar surpresa.
- [x] **T7 CONCLUÍDA (2026-08-25) — `listEditorReviews()`** (`src/lib/review/adminQueries.ts`, +6 testes; suíte em `287 passed | 52 skipped`). **ACHADO levado ao responsável antes de implementar:** `review` tem duas policies de SELECT que se somam por OR — `review_editor_read_own` (0008, próprias OU admin) e `review_public_read` (0005, **qualquer** `status='published'`, concedida também a `authenticated`, não só `anon`). Sem filtro, um editor não-admin receberia as próprias resenhas UNIDAS a toda publicada de QUALQUER outro editor — o mesmo confounder já provado em `rbac-matrix.integration.test.ts:192`. **Decisão:** filtrar `.eq('editor_id', …)` quando o papel não é admin — diferente do padrão que T6 evita (lá seria redundância sobre RLS que já filtra sozinha; aqui é necessário porque a RLS confessadamente une um conjunto maior). Ordenação `updated_at` desc (rascunho recém-editado sobe ao topo). Zero cast: o join `book(title)` infere como objeto único não-nulo (FK `isOneToOne: true`), confirmado quebrando o typecheck de propósito com uma coluna inexistente.
- [ ] **NOTA DE FERRAMENTA — a receita de composição do design é de Zod 3.** O design ilustra `bookInputSchema.innerType().extend(...)`, mas no **Zod 4** `.superRefine()` devolve `ZodObject` (não `ZodEffects`) e **`.innerType()` não existe**; `.omit()` **lança** em schema com refinements; e `.extend()` lança ao SOBRESCREVER chave existente — o caminho correto é **`.safeExtend()`**, que preserva os refinements da base e aplica o override. Tudo verificado por experimento antes de escrever.
- [x] **M3 / T8 — `ReviewForm` (2026-08-25, branch `feat/review-form-t8`).** Primeira UI de escrita do painel: dois `<fieldset>` (*Dados do livro* / *A resenha*), campos repetíveis de `further_reading` com gestão de foco, dois submits que enviam `status` como **dado** (o schema segue sendo derivado no servidor — A-1 intacto). **Gate axe ESTRITO: 0 violação de qualquer impacto E 0 `incomplete`**, nos três estados (repouso / erro / repetíveis) — `incomplete` entra no gate porque regra indecidida não é regra aprovada, e era exatamente o que escondia o contraste **não medido** dos títulos de grupo. Gates: `361 passed | 52 skipped` (+38) · Playwright `30 passed` · typecheck/lint/format limpos. **Três achados registrados na task:** (1) o **React 19 reseta o formulário ao fim de toda action** e apagava a digitação da submissão reprovada — pior no `<select>`, que voltava ao placeholder em silêncio; (2) o `lerFormulario` do T6 **não lia `further_reading`**, então coletá-lo teria criado dado perdido — o leitor virou módulo puro (`src/lib/review/formData.ts`) compartilhado por action e formulário, o que também garante que cliente e servidor leiam os **mesmos nomes de campo**; (3) `reviewer_name` **não é campo** — o RPC o congela de `editor.name` (DD-6), então é exibido, não perguntado. O componente é auditado no `/styleguide` (`ReviewFormDemo`): **nenhuma rota nova** — isso é T10.
- [x] **M3 / T10 — rotas `/admin/resenhas` e `/admin/resenhas/nova` (2026-08-26, branch `feat/review-admin-routes`).** O formulário passou a funcionar de verdade: a lista lê por `listEditorReviews()` (T7) e a criação entrega `createReview` (T6) ao `ReviewForm` (T8) pelas duas props que ele deixou injetáveis. **Nomenclatura resolvida: a rota é `nova`** (ver Backlog). **Gate das rotas provado em navegador**, não por leitura de código: sem sessão, as duas terminam em `/admin/login` e o HTML da resposta não contém nada da lista. Nenhum guard local — o `(protected)/layout.tsx` cobre; as actions do T6 mantêm o gate próprio, que é outro ponto de entrada. **Depois de criar, o editor vai para a lista** com confirmação que RECEBE FOCO (live region não anuncia o que já veio no HTML): ficar no formulário seria armadilha, porque a rota de edição está cortada e um segundo "Salvar" criaria uma segunda resenha. Ordem `revalidatePath` → `redirect` verificada por teste de sequência, e o `redirect` provado NÃO ENGOLIDO (mock que LANÇA, como o Next lança). **Lista em `<table>`** com `caption` + `th scope` de coluna e de linha — o dado é tabular e se compara na vertical. Axe estrito (violations = 0 **e** incomplete = 0) nas três superfícies auditáveis. Gates: `403 passed | 52 skipped` (+42) · Playwright `40 passed` (+10) · typecheck/lint/format/build limpos. **Dois achados:** (1) o T6 exportava `IDLE_STATE` (objeto) de um módulo `'use server'` — inválido desde sempre, silencioso até a T10 importar o módulo a partir de outro `'use server'`, quando o **build quebrou**; só o VALOR desceu para `src/lib/review/formState.ts` e o TIPO segue declarado no action (reexportar o tipo também quebra); (2) `listEditorReviews` ordenava por `updated_at` sem trazê-lo no `select`, então a lista ordenava por um critério invisível — campo acrescentado (acréscimo, não redesenho). **NÃO verificado: o fluxo end-to-end** (criar → aparecer na lista → publicar), que exige sessão de editor e portanto Supabase local — é a TD-02; cada elo está provado em isolamento.

---

## Preferences

**Model Guidance Shown:** 2026-06-07